# 🎯 SWOT Analyzer — Docusaurus UI

Красивый веб-интерфейс для просмотра SWOT-анализов с версионированием.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd swot-docs
npm install
```

### 2. Генерация документации из базы

```bash
# Укажите путь к вашей SQLite базе
npm run generate -- --db ../swot.db
```

### 3. Запуск

**Без авторизации (для разработки):**
```bash
npm start
```

**С авторизацией:**
```bash
# Настройте переменные (опционально)
export SWOT_USER=admin
export SWOT_PASSWORD=mysecretpassword
export PORT=3000

# Запустите
npm run start:auth
```

Откройте http://localhost:3000

## 📁 Структура

```
swot-docs/
├── docs/
│   ├── intro.md           # Главная страница
│   ├── swot/              # SWOT-анализы (генерируются)
│   └── comparisons/       # Сравнения (генерируются)
├── src/
│   ├── components/
│   │   ├── SwotMatrix.jsx         # Матрица 2x2
│   │   ├── StrategicPairs.jsx     # S+O, W+O, S+T, W+T
│   │   └── ComparisonTimeline.jsx # Timeline изменений
│   └── css/
│       └── custom.css     # Стили SWOT
├── scripts/
│   └── generate-docs.js   # Генератор MDX из SQLite
├── server.js              # Прокси с Basic Auth
└── docusaurus.config.js
```

## 🔄 Workflow

1. **Запустить анализ** (Python):
   ```bash
   python swot_analyzer.py company.md --context context.md --db swot.db
   ```

2. **Обновить документацию**:
   ```bash
   npm run generate -- --db ../swot.db
   ```

3. **Просмотреть результаты**:
   - SWOT-матрица с цветовой кодировкой
   - Стратегическое сопоставление
   - История изменений

## 🎨 Возможности

### SWOT-матрица
- 💪 Зелёный — Сильные стороны
- 😰 Красный — Слабые стороны
- 🚀 Синий — Возможности
- ⚠️ Жёлтый — Угрозы

### Стратегии
- **S+O** — Наступательная (использовать силу для возможностей)
- **W+O** — Улучшения (закрыть слабости через возможности)
- **S+T** — Защитная (нейтрализовать угрозы силой)
- **W+T** — Минимизация рисков (критические зоны)

### Сравнения
- Timeline изменений
- Группировка по типу (улучшено/новое/ухудшилось/потеряно)
- Обоснования изменений от LLM

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `SWOT_USER` | `admin` | Логин для авторизации |
| `SWOT_PASSWORD` | `swot2024` | Пароль |
| `PORT` | `3000` | Порт сервера |

### .env файл

```bash
cp .env.example .env
# Отредактируйте .env
```

## 🔐 Безопасность

- Basic Auth защищает все страницы
- Данные хранятся локально в SQLite
- Никаких внешних запросов (кроме npm пакетов)

## 📝 Разработка

```bash
# Запуск в dev режиме
npm start

# Сборка для production
npm run build
npm run serve
```

## 🐛 Troubleshooting

**Ошибка "База данных не найдена"**
```bash
# Проверьте путь
ls -la ../swot.db

# Укажите полный путь
npm run generate -- --db /full/path/to/swot.db
```

**Пустые страницы SWOT**
```bash
# Сначала запустите анализ
python swot_analyzer.py test.md --context context.md

# Потом генерацию
npm run generate -- --db swot.db
```
